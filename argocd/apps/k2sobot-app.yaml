---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k2sobot
  namespace: argo
  # finalizers:
  #   - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/red512/triton-gitops
    targetRevision: HEAD
    path: helm/k2sobot-helm-chart
  destination:
    server: https://kubernetes.default.svc
    namespace: k2so
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - Validate=true
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true